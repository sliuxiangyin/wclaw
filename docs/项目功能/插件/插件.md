# 插件类型


| 插件类型                | 作用                                                             | 模式                                                                  | 说明                                                                                                                                                                |
| ------------------- | -------------------------------------------------------------- | ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `runtime_plugin` | 运行时扩展（如微信桥），负责账号、消息、二维码、会话拉取等核心运行时能力。                          | 不适用（由运行时会话模型驱动）                                                     | 适合常驻运行与后台任务场景：可在 `setup(ctx)` 中注册轮询/定时任务（通过 `scheduler`），并通过 `emit` 持续向宿主上报事件。`runtime_plugin` 默认接入 LLM，并可在策略允许下编排其他插件能力。宿主可存在多个运行时扩展，但当前会优先选择第一个可用实例作为主运行时插件。 |
| `command_plugin`    | 命令型插件，通过 `onCommand` 处理输入命令并返回 `handled/reply/contextSummary`。 | `ephemeral_no_context` / `ephemeral_with_context` / `isolated_chat` | 适合问答、工具调用、流程触发等“按命令执行”的插件。`mode` 决定是否带上下文以及会话隔离方式。                                                                                                                |


## command_plugin 执行模式说明

> 约定：`ephemeral_no_context` / `ephemeral_with_context` / `isolated_chat` 是 `command_plugin` 的执行模式。  
> `command_plugin` 只有在命中该插件命令时（例如 `/echo`）才会执行并返回结果；本质是“命令触发执行”，不是普通自由对话模式。  
> 这三种模式的主要区别是是否带上下文、是否走 LLM，以及是否保持隔离会话。

## command_plugin 三种模式（执行语义）


| 模式                       | 对应作用     | 介绍（简述）                                                                                 |
| ------------------------ | -------- | -------------------------------------------------------------------------------------- |
| `ephemeral_no_context`   | 无上下文命令执行 | 仅在命中插件命令时执行，不读取历史上下文；不接入 LLM。适合确定性、工具型、幂等命令。                                           |
| `ephemeral_with_context` | 带上下文命令执行 | 仅在命中插件命令时执行，会携带当前上下文，并可发送给 LLM 生成结果；适合需要结合当前语境的命令。                                     |
| `isolated_chat`          | 隔离会话命令执行 | 仅在命中插件命令时执行，并使用插件独立上下文会话；可持续在隔离上下文中调用 LLM。该模式必须配置 `isolation.exitCommands` 用于显式退出隔离会话。 |


## runtime_plugin（微信桥）补充

- `runtime_plugin` 不是上述三种 `command_plugin` 执行模式。
- `runtime_plugin` 通常接入 LLM，并承担常驻运行时职责（账号状态、会话拉取、后台任务）。
- 会话数量通过 `sessionProvider.mode` 表达：可为 `single` 或 `multi`；微信桥典型是 `multi`（按账号区分多个会话）。
- 是否可编排其他插件由能力字段控制：`capabilities.crossPluginInvoke`（`none` / `declared` / `policy_granted`）。
- **硬性约束**：`runtime_plugin` 不可编排 `runtime_plugin`；`runtime_plugin` 仅可编排 `command_plugin`。
- 运行时编排场景建议同时声明 `capabilities.orchestration`，用于宿主侧调度与租约治理。

## 术语对照（防混淆）


| 术语                                                                  | 适用对象                         | 含义                                   |
| ------------------------------------------------------------------- | ---------------------------- | ------------------------------------ |
| `ephemeral_no_context` / `ephemeral_with_context` / `isolated_chat` | `command_plugin`             | 命令执行模式（何时执行、是否带上下文、是否走 LLM、是否隔离上下文）。 |
| `sessionProvider.mode=single/multi`                                 | 全部插件（尤其 `runtime_plugin`） | 会话数量形态（一个会话还是多个会话），不是命令执行模式。         |
| `capabilities.llm`                                                  | 全部插件                         | 是否声明可使用 LLM 能力。                      |
| `capabilities.crossPluginInvoke`                                    | 主要用于需要编排的插件                  | 是否可调用其他插件能力（需策略放行）。                  |
| `capabilities.orchestration`                                        | 主要用于 `runtime_plugin`     | 是否进入宿主编排/租约治理能力面。                    |


## executeTurn 返回协议（最新）

- `executeTurn` 统一返回对象：`{ text, continue?, persist? }`，不再推荐返回字符串或 `reply` 字段。
- 默认值约定（由宿主统一解释）：
  - `continue` 默认 `false`（短路优先，直接返回 chat）
  - `persist` 默认 `[]`
- `persist` 含义：追加写入消息到指定会话（可跨会话），每项形如：
  - `{ sessionId, role: "user" | "assistant", content }`
- `continue` 生效范围：
  - 仅在 `runtime_plugin` 路径下读取，用于决定是否继续后续流程（如 LLM）
  - `command_plugin` 的 `ephemeral_no_context` / `ephemeral_with_context` / `isolated_chat` 不读取该字段，仍按模式语义执行

### 示例

```js
return {
  text: `登录成功：${loggedInAccountId}。已创建会话「${accSessionId}」。`,
  continue: false,
  persist: [
    {
      sessionId: accSessionId,
      role: "assistant",
      content: `账号 ${loggedInAccountId} 已连接。发送 /send <联系人> <内容> 可发消息。`
    }
  ]
};
```

