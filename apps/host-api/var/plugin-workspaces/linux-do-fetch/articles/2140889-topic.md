<!-- source: linux.do -->

- 主题 ID: 2140889
- 链接: https://linux.do/t/topic/2140889
- 抓取时间: 2026-05-09T09:27:38.151Z

# 手把手教你搭建中转站：VPS + CPA + NEW API + Docker + Nginx + Cloudflare + SSL 保姆级教程

> 感谢各位佬友的认可，继之前的 CPA 教程后，这次带来更完整的中转站搭建方案。本文不会着重讲解 CPA 的使用，如需了解可参考我的另一篇文章：[拒绝token焦虑 CPA 反代 ChatGPT 保姆级教程](https://linux.do/t/topic/2120257)

## 前提

本教程需要的技术栈（版本仅供参考）：

- **VPS（云服务器）**：2c2g（硅谷地区）
- **Linux**：CentOS 7.6.1810
- **Docker**：26.1.4
- **Docker Compose**：2.27.1
- **CPA**：latest（v6.10.9）
- **NEW API**：latest（v1.0.0-rc.4）
- **Nginx**：latest（1.29.8）

所需工具：
- **FinalShell**：SSH 远程连接工具（[下载地址](https://www.hostbuf.com/)）

## VPS 选择

我使用腾讯云服务器做教学，全程可用国内网访问。有 VPS 的佬友可跳过此步。

### 活动页：[腾讯云最新活动](https://cloud.tencent.com/act?Is=sdk-topnav)

新人优惠，2c2g 一年 99 米。

![](https://cdn3.ldstatic.com/optimized/4X/e/c/4/ec44713d232cf37e7f736500e1db205a5d8711e1_2_690x449.png)

### 购买完毕后进入控制台

![](https://cdn3.ldstatic.com/optimized/4X/e/a/5/ea5302c9721de4684911886bf23bc2c96c48d0af_2_690x235.png)

### 查看服务器 IP

腾讯云默认安全组已打开 22 端口（SSH），这里不再赘述。

![](https://cdn3.ldstatic.com/optimized/4X/5/d/f/5df862c7070dc75d0b42cf02a242e51fdd230f92_2_690x223.png)

### 使用 FinalShell 连接服务器

填写名称、主机 IP、用户名和密码即可连接。FinalShell 可视化操作，省去命令行烦恼。

![](https://cdn3.ldstatic.com/optimized/4X/b/9/c/b9c3cf5162ff7decddccdfda399ae6fe00bafc84_2_690x378.png)

## 对外中转站教程开始

### 一、准备工作

#### 1. 创建文件夹并进入

```bash
mkdir -p /opt/proxy/{cpa/{logs,auths},newapi/{data,logs}} && cd /opt/proxy && touch docker-compose.yml cpa/config.yaml
```

目录结构如下：

```
/opt/proxy/
├── docker-compose.yml
├── cpa/
│   ├── logs/
│   ├── auths/
│   └── config.yaml
└── newapi/
    ├── data/
    └── logs/
```

![](https://cdn3.ldstatic.com/optimized/4X/a/7/d/a7de86914c525d25b2f83831c77a5bb596a5634a_2_690x368.png)

#### 2. 配置 cpa/config.yaml

包含 ⚠️ 的参数需要修改，包含 ❓ 的可选修改，其他照抄。

```yaml
# 服务器绑定主机/接口，默认空字符串同时绑定 IPv4/IPv6
host: ""
# 服务器端口
port: 8317
tls:
  enable: false
remote-management:
  allow-remote: true
  secret-key: "$2a$10$5dHykttqHWAU.WcYFg0qgOXhDoxC1P7wpZ7i2T8Kj9xqwvVRfTUm2"    # ⚠️ 请修改密码
  disable-control-panel: false
auth-dir: "~/.cli-proxy-api"
api-keys:
  - sk-8SGCShq021BAFgpBE  # ⚠️ 请修改秘钥
debug: true
logging-to-file: true
logs-max-total-size-mb: 100
request-retry: 3
max-retry-interval: 30
routing:
  strategy: "round-robin"
ws-auth: false
usage-statistics-enabled: false
```

#### 3. 配置 docker-compose.yml

```yaml
services:
  new-api:
    image: calciumion/new-api:latest
    container_name: new-api
    restart: always
    command: --log-dir /app/logs
    ports:
      - '3000:3000'
    volumes:
      - ./newapi/data:/data
      - ./newapi/logs:/app/logs
    environment:
      - SQL_DSN=postgresql://root:123456@postgres:5432/new-api # ⚠️ 请修改密码
      - REDIS_CONN_STRING=redis://redis
      - TZ=Asia/Shanghai
      - ERROR_LOG_ENABLED=true
      - BATCH_UPDATE_ENABLED=true
    depends_on:
      - redis
      - postgres
      - cpa
    healthcheck:
      test: ['CMD-SHELL', "wget -q -O - http://localhost:3000/api/status | grep -o '\"success\":\\s*true' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3

  redis:
    image: redis:latest
    container_name: redis
    restart: always

  postgres:
    image: postgres:15
    container_name: postgres
    restart: always
    environment:
      POSTGRES_USER: root # ❓ 设置用户名
      POSTGRES_PASSWORD: 123456 # ⚠️ 设置密码
      POSTGRES_DB: new-api
    volumes:
      - pg_data:/var/lib/postgresql/data

  cpa:
    image: eceasy/cli-proxy-api:latest
    container_name: cpa
    volumes:
      - ./cpa/config.yaml:/CLIProxyAPI/config.yaml
      - ./cpa/auths:/root/.cli-proxy-api
      - ./cpa/logs:/CLIProxyAPI/logs
    restart: always

volumes:
  pg_data:
```

#### 4. 将认证文件放入 /opt/proxy/cpa/auths 目录

![](https://cdn3.ldstatic.com/original/4X/f/0/8/f085b0d0c4305dd2a35a5d6a7e51fda8b4c4a0b4.png)

#### 5. 打开 Linux 防火墙 3000 端口

```bash
# 查看防火墙状态
systemctl status firewalld
# 启动防火墙
systemctl start firewalld
# 设置开机自启
systemctl enable firewalld
# 开放 3000 端口
firewall-cmd --zone=public --add-port=3000/tcp --permanent && firewall-cmd --reload && firewall-cmd --list-ports
```

#### 6. 打开云服务器安全组

以腾讯云为例，进入控制台添加规则，开放 3000 端口。

![](https://cdn3.ldstatic.com/optimized/4X/d/1/8/d1831b90e4cd7ad8422acb6b6989836b8fe1ffe6_2_625x500.png)

![](https://cdn3.ldstatic.com/optimized/4X/f/4/d/f4d27a3d5af90b6e6ee14f13c3d78cb7985fa5bf_2_690x247.png)

![](https://cdn3.ldstatic.com/optimized/4X/6/4/a/64a95b16f3502348c530084e86169f409c73095a_2_690x252.png)

### 二、执行 Docker Compose

在 `/opt/proxy` 目录下执行：

```bash
docker compose up -d
```

启动成功输出如下：

```
[+] Running 6/6
 ✔ Network proxy_default   Created
 ✔ Volume "proxy_pg_data"  Created
 ✔ Container cpa           Started
 ✔ Container postgres      Started
 ✔ Container redis         Started
 ✔ Container new-api       Started
```

**PS：重启报错处理**

若 `docker compose down` 后重启报错，先重启 Docker：

```bash
systemctl restart docker
docker compose up -d
```

### 三、访问中转站

通过 `http://ip:3000` 即可访问。

![](https://cdn3.ldstatic.com/optimized/4X/f/8/5/f85e2fded889f2d445af9b7e6fc341f5158f57b2_2_690x324.png)

### 四、NEW API 配置

#### 1. 登录

使用管理员账号登录，即可看到管理菜单。

#### 2. 渠道管理

添加渠道，填写 CPA 的 API Key 和地址（`http://cpa:8317`）。

![](https://cdn3.ldstatic.com/optimized/4X/a/6/c/a6cadc5ea4dbfabd35d7c86eccb581a074ce11ac_2_690x318.png)

#### 3. 系统管理 - 设置模型价格

根据报错提示配置价格。

![](https://cdn3.ldstatic.com/optimized/4X/d/d/2/dd244e9a803c2e993a5b319c8a32e4d030b6455d_2_690x288.png)

#### 4. 配置令牌

创建令牌供客户端使用。

![](https://cdn3.ldstatic.com/optimized/4X/f/c/8/fc81e0e507c22f149c55887ddacc551ef928c218_2_690x415.png)

#### 5. 本地连接测试

使用 CC Switch 测试，配置令牌后即可正常使用。

### 五、域名配置

#### 1. 使用 Cloudflare 代理域名

在 Cloudflare 添加域名，并将 DNS 服务器修改为 Cloudflare 提供的地址。

#### 2. 添加 Nginx 配置

创建 Nginx 目录和文件：

```bash
mkdir -p /opt/proxy/nginx/{ssl,logs,conf.d} && cd /opt/proxy && touch nginx/nginx.conf nginx/conf.d/api.conf nginx/ssl/api.pem nginx/ssl/api.key && chmod 600 ./nginx/ssl/api.key
```

**nginx.conf** 直接复制粘贴：

```nginx
user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
    use epoll;
    multi_accept on;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';
    access_log /var/log/nginx/access.log main;
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    client_max_body_size 100m;
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript
               application/json application/javascript
               application/xml+rss application/rss+xml
               font/truetype font/opentype
               application/vnd.ms-fontobject image/svg+xml;
    include /etc/nginx/conf.d/*.conf;
}
```

**api.conf** 需修改域名和 SSL 证书路径：

```nginx
server {
    listen 80;
    server_name api.wudike.online;  # ⚠️ 你的域名
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name api.wudike.online;  # ⚠️ 你的域名

    ssl_certificate /etc/nginx/ssl/api.pem;
    ssl_certificate_key /etc/nginx/ssl/api.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    add_header Strict-Transport-Security "max-age=63072000" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    access_log /var/log/nginx/api-access.log main;
    error_log /var/log/nginx/api-error.log;
    client_max_body_size 100m;

    location / {
        proxy_pass http://new-api:3000;  # ⚠️ 你的应用服务名
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
        proxy_buffering off;
        proxy_cache off;
        proxy_cache_bypass $http_upgrade;
    }

    location /nginx-health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}
```

**SSL 证书** 从 Cloudflare 获取。

#### 3. 配置 DNS 解析

在 Cloudflare 添加 A 记录，指向 VPS IP。

#### 4. 打开防火墙 80 和 443 端口

```bash
firewall-cmd --permanent --add-port=443/tcp && firewall-cmd --permanent --add-port=80/tcp && firewall-cmd --reload
firewall-cmd --permanent --remove-port=3000/tcp && firewall-cmd --reload
```

#### 5. 修改 docker-compose.yml 添加 Nginx

```yaml
services:
  new-api:
    # ... 其他配置不变
    ports:
      - '3000'  # 改为 expose，不再对外暴露
    networks:
      - newapi-network

  # ... redis, postgres, cpa 添加 networks

  nginx:
    image: nginx:alpine
    container_name: nginx
    restart: always
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
      - ./nginx/logs:/var/log/nginx
    depends_on:
      - new-api
    networks:
      - newapi-network

volumes:
  pg_data:

networks:
  newapi-network:
    driver: bridge
```

#### 6. 重新启动 Docker Compose

```bash
docker compose down
docker compose up -d
```

访问 `https://api.wudike.online` 即可通过 HTTPS 访问中转站。

![](https://cdn3.ldstatic.com/optimized/4X/f/8/8/f88db5032a3c5c6758105454da872604a7f18785_2_690x357.jpeg)

## 参考文章

- [全平台 Docker 部署 CPA 灵活定制指南](https://juejin.cn/post/7617781226364092450#heading-8)
- [【教程】2026版 小白也能看懂的自建Cloudflare临时邮箱教程](https://linux.do/t/topic/1666961)